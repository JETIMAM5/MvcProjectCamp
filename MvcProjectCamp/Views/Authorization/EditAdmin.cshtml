@model EntityLayer.Concrete.Admin
@{
    ViewBag.Title = "EditAdmin";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<br />
<h2>Admin Updating Page</h2>
@using (Html.BeginForm("EditAdmin", "Auhtorization", FormMethod.Post))
{
    <div class="form-group">

        @Html.HiddenFor(x => x.AdminID, new { @class = "form-control" })
        <br />
        @Html.Label("Admin User Name")
        @Html.TextBoxFor(x => x.AdminUserName, new { @class = "form-control" })
        <br />
        @Html.Label("Password")
        @Html.TextAreaFor(x => x.AdminPassword, new { @class = "form-control" })
        <br />
        @Html.Label("Role")
        @Html.TextAreaFor(x => x.AdminRole, new { @class = "form-control" })
        <br />
        <button class="btn btn-succes">Update</button>
    </div>
}

